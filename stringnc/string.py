# ==================== PHẦN 4: XỬ LÝ CHUỖI NÂNG CAO ====================

# --- CƠ BẢN ---
# 1. Nhập chuỗi và in ra.
# 2. In độ dài của chuỗi (dùng len).
# 3. Duyệt từng ký tự trong chuỗi bằng for.
# 4. Đếm số ký tự trong chuỗi (không dùng len).
# 5. Đếm số lần xuất hiện ký tự 'a' trong chuỗi.
# 6. Đếm số nguyên âm trong chuỗi.
# 7. Kiểm tra chuỗi có rỗng không.
# 8. Kiểm tra chuỗi toàn chữ hoa.
# 9. Kiểm tra chuỗi toàn chữ thường.
# 10. Kiểm tra chuỗi có phải viết hoa chữ cái đầu không (title).

# --- SLICING ---
# 11. Lấy 5 ký tự đầu tiên của chuỗi.
# 12. Lấy 5 ký tự cuối cùng của chuỗi.
# 13. Lấy các ký tự ở vị trí chẵn.
# 14. Lấy các ký tự ở vị trí lẻ.
# 15. Đảo ngược chuỗi bằng slicing.
# 16. Lấy chuỗi con từ vị trí i đến j.
# 17. Cắt bỏ 2 ký tự đầu và cuối chuỗi.
# 18. In chuỗi bỏ qua mỗi 2 ký tự.
# 19. Lấy ký tự đầu tiên và cuối cùng.
# 20. Ghép 2 chuỗi con bằng slicing.

# --- HÀM XỬ LÝ CHUỖI ---
# 21. Chuyển chuỗi thành chữ hoa (upper).
# 22. Chuyển chuỗi thành chữ thường (lower).
# 23. Viết hoa chữ cái đầu tiên (capitalize).
# 24. Viết hoa chữ cái đầu mỗi từ (title).
# 25. Xóa khoảng trắng đầu cuối (strip).
# 26. Thay thế ký tự trong chuỗi (replace).
# 27. Tách chuỗi thành list theo khoảng trắng (split).
# 28. Nối list thành chuỗi (join).
# 29. Tìm vị trí ký tự đầu tiên trong chuỗi (find).
# 30. Kiểm tra chuỗi bắt đầu bằng 'a' (startswith).

# --- KIỂM TRA ĐIỀU KIỆN ---
# 31. Kiểm tra chuỗi kết thúc bằng 'z' (endswith).
# 32. Kiểm tra chuỗi có chứa số không (any c.isdigit).
# 33. Kiểm tra chuỗi có chứa ký tự đặc biệt không.
# 34. Kiểm tra chuỗi có phải toàn chữ cái không (isalpha).
# 35. Kiểm tra chuỗi có phải toàn số không (isdigit).
# 36. Kiểm tra chuỗi có phải alphanumeric không (isalnum).
# 37. Kiểm tra chuỗi có phải ASCII không (isascii).
# 38. Kiểm tra chuỗi có phải khoảng trắng không (isspace).
# 39. Kiểm tra chuỗi có phải định danh hợp lệ không (isidentifier).
# 40. Kiểm tra chuỗi có thể in ra màn hình không (isprintable).

# --- BÀI TẬP ỨNG DỤNG ---
# 41. Đếm số chữ hoa và chữ thường trong chuỗi.
# 42. Đếm số chữ số trong chuỗi.
# 43. Loại bỏ toàn bộ khoảng trắng trong chuỗi.
# 44. Loại bỏ toàn bộ dấu chấm câu trong chuỗi.
# 45. Đếm số từ trong chuỗi (dùng split).
# 46. Đếm số lần xuất hiện của từng từ trong chuỗi.
# 47. Tìm từ dài nhất trong chuỗi.
# 48. Tìm từ ngắn nhất trong chuỗi.
# 49. Ghép ngược các từ trong chuỗi.
# 50. Kiểm tra chuỗi có phải palindrome (đối xứng).

# --- BIỂU THỨC CHÍNH QUY (regex) ---
# 51. Kiểm tra chuỗi có chứa số điện thoại (10 chữ số).
# 52. Kiểm tra email có hợp lệ không.
# 53. Tìm tất cả số trong chuỗi.
# 54. Tìm tất cả chữ cái trong chuỗi.
# 55. Kiểm tra chuỗi có chứa ngày (dd/mm/yyyy).
# 56. Thay thế tất cả số trong chuỗi bằng ký tự '*'.
# 57. Kiểm tra mật khẩu mạnh (chứa hoa, thường, số, ký tự đặc biệt).
# 58. Cắt chuỗi thành danh sách từ bằng regex.
# 59. Kiểm tra chuỗi có chứa từ khóa "python" không (regex).
# 60. Đếm số lần từ "python" xuất hiện trong chuỗi (regex).
