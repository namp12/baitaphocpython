# ==================== PHẦN 6: FILE & DỮ LIỆU ====================

# --- ĐỌC / GHI FILE CƠ BẢN ---
# 1. Tạo file "hello.txt" và ghi vào "Xin chào Python".
# 2. Đọc nội dung file "hello.txt" và in ra màn hình.
# 3. Ghi nhiều dòng văn bản vào file.
# 4. Đọc file theo từng dòng (dùng for).
# 5. Đọc toàn bộ file bằng read().
# 6. Đọc file bằng readline() từng dòng.
# 7. Ghi thêm dữ liệu (append) vào file.
# 8. Kiểm tra file có tồn tại trước khi mở.
# 9. Đếm số dòng trong file.
# 10. Đếm số từ trong file.

# --- XỬ LÝ NỘI DUNG FILE ---
# 11. Đếm số ký tự trong file.
# 12. Tìm từ dài nhất trong file.
# 13. Tìm từ xuất hiện nhiều nhất trong file.
# 14. Thay thế một từ trong file (replace).
# 15. Đảo ngược nội dung từng dòng trong file.
# 16. Lọc ra những dòng bắt đầu bằng chữ "A".
# 17. Lọc ra những dòng có độ dài > 10 ký tự.
# 18. Xóa dòng trống trong file.
# 19. Viết hoa toàn bộ nội dung file.
# 20. Viết thường toàn bộ nội dung file.

# --- VỚI CSV ---
# 21. Tạo file CSV lưu danh sách sinh viên (name, age, grade).
# 22. Đọc dữ liệu từ file CSV và in ra.
# 23. Thêm một sinh viên mới vào file CSV.
# 24. Tính điểm trung bình từ file CSV.
# 25. Tìm sinh viên có điểm cao nhất trong CSV.
# 26. Tìm sinh viên có điểm thấp nhất trong CSV.
# 27. Đếm số sinh viên trong CSV.
# 28. Lọc ra sinh viên có điểm >= 8.
# 29. Sắp xếp sinh viên theo điểm giảm dần.
# 30. Xuất danh sách sinh viên giỏi sang file mới.

# --- VỚI JSON ---
# 31. Tạo file JSON lưu thông tin một sinh viên.
# 32. Đọc file JSON và in ra thông tin.
# 33. Ghi danh sách sinh viên vào file JSON.
# 34. Thêm một sinh viên mới vào JSON.
# 35. Cập nhật tuổi của sinh viên trong JSON.
# 36. Xóa một sinh viên theo tên trong JSON.
# 37. Đếm số sinh viên trong JSON.
# 38. Tìm sinh viên có điểm >= 9 trong JSON.
# 39. Xuất danh sách sinh viên từ JSON ra CSV.
# 40. Đọc file JSON phức tạp (nested dictionary).

# --- XỬ LÝ NGOẠI LỆ (try-except) ---
# 41. Xử lý lỗi khi mở file không tồn tại.
# 42. Xử lý lỗi khi chia cho 0.
# 43. Xử lý lỗi khi nhập số không hợp lệ.
# 44. Xử lý lỗi khi đọc JSON sai định dạng.
# 45. Xử lý lỗi khi đọc CSV rỗng.
# 46. Xử lý lỗi khi chuyển đổi kiểu dữ liệu (str → int).
# 47. Thử nhiều except khác nhau.
# 48. Dùng finally để đóng file sau khi đọc.
# 49. Tạo ngoại lệ tự định nghĩa (custom exception).
# 50. Raise exception khi nhập điểm < 0.

# --- NÂNG CAO ---
# 51. Ghi danh sách số nguyên vào file, sau đó đọc và tính tổng.
# 52. Ghi bảng cửu chương từ 2–9 vào file.
# 53. Lưu danh sách từ điển (dictionary) vào JSON.
# 54. Đọc nhiều dòng từ file, lưu vào list.
# 55. Ghi log hoạt động của chương trình vào file log.txt.
# 56. Đếm số lần mỗi ký tự xuất hiện trong file.
# 57. Ghép nội dung từ 2 file thành 1 file mới.
# 58. So sánh nội dung 2 file, in ra dòng khác nhau.
# 59. Tạo chương trình quản lý danh bạ lưu vào file CSV.
# 60. Tạo chương trình quản lý tài khoản ngân hàng lưu vào JSON.
